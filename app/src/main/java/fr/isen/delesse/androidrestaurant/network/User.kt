package fr.isen.delesse.androidrestaurant.network

class User(private var name: String,
           private var firstname: String,
           private var address: String,
           private var emmail:String,
           private var password: String) {

}